var mesh-slice-polygon;
if (typeof require !== "undefined") {
  mesh-slice-polygon = require("../mesh-slice-polygon.js");
} else {
  mesh-slice-polygon = window.mesh-slice-polygon;
}

var ok = function(a, msg) { if (!a) throw new Error(msg || "not ok"); };
var eq = function(a, b) { if (a!==b) throw new Error(a + " !== " + b); };

describe('mesh-slice-polygon', function() {
  describe('#', function() {
    it('', function() {
      
    });
  });
});
